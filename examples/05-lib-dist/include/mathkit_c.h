/* The TEXT interface: published whole, never trimmed.
   A source distribution of this package puts every header here on its
   consumers' include path, so a binary package that shipped a subset would
   give the same library a different public surface. */
#ifdef __cplusplus
extern "C" {
#endif

int mathkit_add(int a, int b);

#ifdef __cplusplus
}
#endif
